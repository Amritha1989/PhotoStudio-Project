---
# Source: photo-studio/templates/ps-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photostudio-app-ps-pvc
  namespace: final
spec:
  accessModes:
        - ReadWriteMany
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 5Gi
