---
# Source: photo-studio/templates/mongodb-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photostudio-app-mongodb-pvc
  namespace: final
spec:
  accessModes:
        - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
